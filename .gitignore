# Cluster-only data and checkpoint locations inside the working tree.
/code/msi/data
/code/msi/checkpoints/
/code/msi/weights/
# Routine Slurm output stays local/cluster-side. Selected evidence belongs in
# code/msi/reproducibility instead.
/code/msi/logs/

# Raw MSI inputs and archives are stored under /datasets on the cluster.
*.h5
*.imzML
*.ibd
*.zip
*.7z
__pycache__/
*.py[cod]
.uv-cache/
.pym2aia_prefs.xml
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.venv/
venv/
Thumbs.db
.DS_Store
*.swp
*~
/tmp/
# Trained model weights belong in cluster/external storage, not Git.
*.pt
*.pth
*.ckpt
# Personal meeting notes are kept in Obsidian rather than in Git.
/docs/meetings/
