# Cluster-only data and checkpoint locations inside the working tree.
/code/msi/data/
/code/msi/checkpoints/
/code/msi/weights/

# Raw MSI inputs and archives are stored under /datasets on the cluster.
*.h5
*.imzML
*.ibd
*.zip
*.7z
__pycache__/
*.py[cod]
.uv-cache/
.pym2aia_prefs.xml
# Trained model weights belong in cluster/external storage, not Git.
*.pt
*.pth
*.ckpt
# Personal learning notes are kept locally rather than in the research history.
/docs/my-understanding.txt
/docs/meetings/